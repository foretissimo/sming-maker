# Antigravity & Gemini Agent Rules for sming-maker

## 🚨 Mandatory Pre-Task Step
Before starting any coding task, inspecting data files, or making edits in this project, you MUST ALWAYS run:
```bash
git pull origin main
```
Reason: The sound team (음총팀) updates `src/data/songs.json` and `src/data/recommendedPlaylist.json` directly on GitHub `main` branch from the web editor.
